<%= form_for(@item_review) do |f| %>
  <% if @item_review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item_review.errors.count, "error") %> prohibited this item_review from being saved:</h2>

      <ul>
      <% @item_review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :item_id %><br>
    <%= f.number_field :item_id %>
  </div>
  <div class="field">
    <%= f.label :handlename %><br>
    <%= f.text_field :handlename %>
  </div>
  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_area :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
